{% extends 'base.html.twig' %}

{% block body %}
    <h1>Producttypefield</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ productTypeField.id }}</td>
            </tr>
            <tr>
                <th>Producttypeid</th>
                <td>{{ productTypeField.productTypeId }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ productTypeField.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('producttypefields_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('producttypefields_edit', { 'id': productTypeField.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
